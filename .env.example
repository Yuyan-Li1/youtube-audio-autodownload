# YouTube Audio Downloader Configuration
# Copy this file to .env and fill in your values

# =============================================================================
# REQUIRED SETTINGS
# =============================================================================

# YouTube Data API v3 key
# Get one at: https://developers.google.com/youtube/v3/getting-started
YOUTUBE_API_KEY=your_api_key_here

# Directory to move completed downloads to
# This is where your podcast app should look for sideloaded files
# Example for Castro on macOS:
TARGET_DIRECTORY=/Users/yourusername/Library/Mobile Documents/iCloud~co~supertop~castro/Documents/Sideloads

# =============================================================================
# OPTIONAL SETTINGS (with defaults)
# =============================================================================

# Temporary directory for downloads before moving (default: ./downloads/)
# DOWNLOAD_DIRECTORY=./downloads/

# How many days back to check for new videos (default: 7)
# Higher values = more API quota used, but safer if cron job misses runs
# LOOKBACK_DAYS=7

# Logging level: DEBUG, INFO, WARNING, ERROR, CRITICAL (default: INFO)
# Use DEBUG for troubleshooting cron issues
# LOG_LEVEL=INFO

# Log file path (optional, default: logs to stderr only)
# Useful for cron jobs since they don't show stdout
# LOG_FILE=./logs/downloader.log

# History file path (default: ./download_history.json)
# HISTORY_FILE=./download_history.json
