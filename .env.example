# YouTube Audio Downloader Configuration
# Copy this file to .env and fill in your values

# =============================================================================
# REQUIRED SETTINGS
# =============================================================================

# YouTube Data API v3 key
# Get one at: https://developers.google.com/youtube/v3/getting-started
YOUTUBE_API_KEY=your_api_key_here

# Directory to move completed downloads to
# This is where your podcast app should look for sideloaded files
# Example for Castro on macOS:
TARGET_DIRECTORY=/Users/yourusername/Library/Mobile Documents/iCloud~co~supertop~castro/Documents/Sideloads

# =============================================================================
# OPTIONAL SETTINGS (with defaults)
# =============================================================================

# Temporary directory for downloads before moving (default: ./downloads/)
# DOWNLOAD_DIRECTORY=./downloads/

# How many days back to check for new videos (default: 7)
# Higher values = more API quota used, but safer if cron job misses runs
# LOOKBACK_DAYS=7

# Logging level: DEBUG, INFO, WARNING, ERROR, CRITICAL (default: INFO)
# Use DEBUG for troubleshooting cron issues
# LOG_LEVEL=INFO

# Log file path (optional, default: logs to stderr only)
# Useful for cron jobs since they don't show stdout
# LOG_FILE=./logs/downloader.log

# History file path (default: ./download_history.json)
# HISTORY_FILE=./download_history.json

# =============================================================================
# HISTORY & CLEANUP SETTINGS
# =============================================================================

# Maximum age (in days) to keep entries in download history (default: 90)
# Older entries are automatically cleaned up to prevent unbounded growth
# HISTORY_MAX_AGE_DAYS=90

# =============================================================================
# AUDIO FILE SETTINGS
# =============================================================================

# Comma-separated list of audio file extensions to move to target directory
# (default: .m4a,.mp3,.opus,.webm,.aac,.ogg,.wav,.flac)
# AUDIO_EXTENSIONS=.m4a,.mp3,.opus,.webm,.aac,.ogg,.wav,.flac

# =============================================================================
# API RATE LIMITING
# =============================================================================

# Delay in seconds between YouTube API calls for different channels (default: 1.0)
# Helps avoid hitting YouTube API rate limits when monitoring many channels
# API_RATE_LIMIT_DELAY=1.0

# =============================================================================
# DOWNLOAD RETRY SETTINGS
# =============================================================================

# Maximum number of retry attempts for failed downloads (default: 3)
# DOWNLOAD_MAX_RETRIES=3

# Initial backoff delay in seconds for retry attempts (default: 2.0)
# Uses exponential backoff: 2s, 4s, 8s, etc.
# DOWNLOAD_INITIAL_BACKOFF=2.0

# =============================================================================
# DRY RUN / TESTING SETTINGS
# =============================================================================

# Number of mock videos to generate per channel in dry run mode (default: 2)
# DRY_RUN_MOCK_VIDEO_COUNT=2
